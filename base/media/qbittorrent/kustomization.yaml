apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - pvc.yaml
# NOTE: The vpn-credentials Secret must be created manually.
# It is NOT stored in git. See README.md for details.
