apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - service.yaml
  - servicemonitor.yaml
# Secret: exportarr-credentials — managed via SealedSecrets in envs/*/monitoring/sealed-secrets/
