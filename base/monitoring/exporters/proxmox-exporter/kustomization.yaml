apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - service.yaml
  - servicemonitor.yaml
# Secret: proxmox-exporter-config — managed via SealedSecrets in envs/*/monitoring/sealed-secrets/
